---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: xonery-tls
  namespace: xonery-ns
spec:
  secretName: xonery-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  commonName: xonery.dev
  dnsNames:
    - xonery.dev
    # Subdomains
    - podinfo.xonery.dev
