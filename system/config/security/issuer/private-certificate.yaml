---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cluster-tls
  namespace: envoy-gateway-system
spec:
  secretName: cluster-tls
  issuerRef:
    name: ${cluster_issuer}
    kind: ClusterIssuer
  dnsNames:
    - "*.${cluster_domain}"
