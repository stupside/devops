apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - release.yaml
  - secret.yaml
  # oauth2-client.yaml, oauth2-client-secret.yaml, route.yaml
  # moved to config/security/oidc/ (deployed after Hydra CRDs exist)
