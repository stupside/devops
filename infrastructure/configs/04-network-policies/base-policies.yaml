---
apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: base-allow-core
spec:
  description: "Allow core cluster connectivity (DNS, API Server, Health Checks)"
  endpointSelector: {}
  egress:
    # Allow DNS
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s:k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
            - port: "53"
              protocol: TCP
    # Allow API Server
    - toEntities:
        - kube-apiserver
    # Allow Health Checks (Kubelet)
    - toEntities:
        - host
        - remote-node
  ingress:
    # Allow Health Checks (Kubelet)
    - fromEntities:
        - host
        - remote-node
---
apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: base-default-deny
spec:
  description: "Default deny ingress and egress for all pods"
  endpointSelector:
    matchExpressions:
      - key: io.kubernetes.pod.namespace
        operator: NotIn
        values: [kube-system, flux-system]
  ingress: [] # Explicitly deny all ingress
  egress: [] # Explicitly deny all egress
