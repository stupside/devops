apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: discord
  namespace: flux-system
spec:
  type: discord
  # Secret created manually or via SOPS in 01-secrets
  secretRef:
    name: discord-webhook-url
---
# Example Secret (Encrypted with SOPS)
# apiVersion: v1
# kind: Secret
# metadata:
#   name: discord-webhook-url
#   namespace: flux-system
# stringData:
#   address: https://discord.com/api/webhooks/...
