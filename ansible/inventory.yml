k3s_cluster:
  children:
    server:
      hosts:
        bigboy:
          ansible_connection: ssh
          ansible_host: 158.178.195.110
          ansible_ssh_private_key_file: ~/.ssh/bigboy.key
    agent:
      hosts:
  vars:
    ansible_port: 22
    ansible_user: ubuntu
    ansible_connection: ssh

    k3s_version: v1.26.9+k3s1
